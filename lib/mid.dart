import 'package:flutter/material.dart';

class Mid extends StatelessWidget {
  const Mid({super.key});


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Mid Priority'),
      ),
      body: const Center(
        child: Text(
          '',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
